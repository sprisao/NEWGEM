import {StyleSheet, Text, View} from "react-native";
import React from "react";

const FireTest = () => {
  return (
    <View>
      <Text>Practice TDD</Text>
    </View>
  );
};

export default FireTest;

const styles = StyleSheet.create({});
